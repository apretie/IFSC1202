#There's the reduced form of range() to range(max_value). 
# That is exactly the same as range(0, max_value), i.e. range(max_value) generates the sequence of numbers :
# 0, 1, 2, ..., max_value - 1.

for i in range(5):
    print(i)